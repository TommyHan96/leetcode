package com.test.leetcode

class DivideAndConquerSolutions {
  //53.最大子序和
  //分治法实现
//    def recursiveFunction(nums: Array[Int], bottom: Int, top: Int): Int ={
//      if(bottom == top) return nums(bottom)
//
//      var mid = (bottom + top) / 2
//      var bottomSum = recursiveFunction(nums,bottom,mid)
//      var topSum = recursiveFunction(nums,mid+1,top)
//      var crossSum = 0
//
//      math.max(bottomSum,math.max(topSum,crossSum))
//    }
//    def crossSum(nums: Array[Int], bottom: Int, top: Int,mid: Int): Int ={
//      if(bottom == top) return nums(bottom)
//      var leftSubsum = Int.MinValue
//      var currSum = 0
//      for (i <- bottom to mid){
//
//      }
//    }
//    def maxSubArray(nums: Array[Int]): Int = {
//      val len = nums.length
//      if(len == 1) return nums(len - 1)
//      else {
//
//      }
//
//    }

}
